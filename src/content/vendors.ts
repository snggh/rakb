export type Vendor = {
  name: string;
  category: string;
  kind: string;
  notes?: string;
  logo?: string;
};

/** Stub for the future vendor & artisan directory. */
export const vendors: Vendor[] = [];
